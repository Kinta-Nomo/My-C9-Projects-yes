
#based on Python 2

define den_to_bin(denary):
    assign '' to binary
    
    while denary is bigger than 0:
        add string of denary modulo 2 to the binary
        assign integer denary devided by 2 to denary
        
    return integer of binary

print den_to_bin(9)
